// Auth is handled directly through Supabase client
// This file is kept for consistency but auth operations use supabase.auth directly
export const authApi = {
  // These methods are not used - auth is handled via Supabase client
  // Kept for potential future use or consistency
};

