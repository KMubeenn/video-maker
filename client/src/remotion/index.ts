/**
 * Remotion Root Entry Point
 * Required by Remotion CLI
 */

import { registerRoot } from "remotion";
import { RemotionRoot } from "./compositions/RankingVideo.composition";

registerRoot(RemotionRoot);
